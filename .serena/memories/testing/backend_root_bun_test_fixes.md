Root `bun test` can execute backend Bun suites directly and expose module-cache-sensitive mocks. The products route test at `packages/backend/src/modules/products/__tests__/products.test.ts` should avoid mocking `../products.service` because that wrapper re-exports through `index.ts`; instead, dynamically import `createProductsRoute` from `interface/http/products.route` after mocking `#plugins/auth.plugin` and inject a local `mockService`. Also, `src/libraries/audit-logger.ts` now short-circuits when `process.env.DATABASE_URL` is missing so test runs without a configured database do not emit repeated audit-log connection errors.