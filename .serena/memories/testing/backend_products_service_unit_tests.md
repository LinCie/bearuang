For backend clean-architecture modules, a practical unit-test seam is the application service factory (e.g. `src/modules/products/application/products.service.ts`). The products module now has `products.service.test.ts` that mocks `ProductRepository` and `ProductCategoryTreeRepository` and verifies each service method maps inputs to the correct repository/use-case call. This covers the one orchestration path with real logic (`listProductsByCategoryTree`) without duplicating route tests. Backend `package.json` test script was also updated to `bun test`, so backend tests can run through the workspace test workflow.